auth:
  postgresPassword: "postgres"
  database: "postgres"

primary:
  persistence:
    enabled: true
    size: 20Gi
    storageClass: local-path

readReplicas:
  replicaCount: 1

metrics:
  enabled: true